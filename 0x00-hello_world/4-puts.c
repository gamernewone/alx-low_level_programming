#include <stdio.h>
#include <string.h>
/**
 * main - print a string
 *
 * Return: return 0
 */
int main(void)
{
    /* my first program in C */
char str1[52];

strcpy(str1, "\"Programming is like building a multilingual puzzle");

puts(str1);

return (0);
}
